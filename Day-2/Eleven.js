// Write a program that uses the && operator to combine two conditions and log the result to the console.
var isWeekend = true;
var isSunny = true;
if (isWeekend && isSunny) {
  console.log("It's Weekend and Sunny....!!");
} else {
  console.log("It is not Sunny....!!");
}
